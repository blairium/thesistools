[project]
name = "thesistools"
version = "0.1.1"
description = "Tools for my thesis"
readme = "README.md"
authors = [{ name = "Blair Haydon", email = "b.haydon@latrobe.edu.au" }]
requires-python = ">=3.12.9"
dependencies = ["numpy>=2", "matplotlib >=3"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
